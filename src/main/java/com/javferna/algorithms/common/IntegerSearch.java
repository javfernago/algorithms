package com.javferna.algorithms.common;

/**
 * Interface that defines a method to find an integer number in an array of
 * integer numbers.
 *
 * If it finds the number, it returns the index of the array where the number is stored
 * Otherwise, it returns -1
 */

public interface IntegerSearch {
    int find (int[] data, int number);
}
